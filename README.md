# cold-start-personalization-engine
A production-aware cold-start recommendation system that uses transformer-based semantic embeddings and density-based unsupervised clustering to infer user intent from a single textual input, with hybrid fallback logic and explainable recommendations.
